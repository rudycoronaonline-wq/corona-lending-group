---
/**
 * DSCR loan page: serves _dscr-content.html verbatim (no Astro processing of inner HTML).
 */
export const prerender = true;
import dscrContentHtml from './_dscr-content.html?raw';
---
<Fragment set:html={dscrContentHtml} />
