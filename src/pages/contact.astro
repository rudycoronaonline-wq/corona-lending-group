---
/**
 * Contact page: serves _contact-content.html verbatim (no Astro processing of inner HTML).
 */
export const prerender = true;
import contactContentHtml from './_contact-content.html?raw';
---
<Fragment set:html={contactContentHtml} />
